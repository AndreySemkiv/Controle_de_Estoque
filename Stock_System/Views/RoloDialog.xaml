<Window x:Class="EstoqueRolos.Views.RoloDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dados da Bobina"
        Height="624" Width="460"
        WindowStartupLocation="CenterOwner"
        Background="#0D0D0D"
        ResizeMode="NoResize"
        Icon="/Views/Logo.png">

    <Window.Resources>
        <!-- Estilo de TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <!-- Botão primário -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="4,0,4,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E40AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Botão secundário -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4B5563"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Border Background="#1A1A1A"
                CornerRadius="16"
                Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20"
                                  ShadowDepth="0"
                                  Opacity="0.6"
                                  Color="#2563EB"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Título -->
                <TextBlock Text="📄 Dados da Bobina"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"/>

                <!-- Formulário -->
                <StackPanel Grid.Row="1" Margin="0,20,0,0">
                    <Label Content="Código" Foreground="White"/>
                    <TextBox x:Name="txtCode"/>

                    <Label Content="Descrição" Foreground="White"/>
                    <TextBox x:Name="txtDescricao"/>

                    <Label Content="Milimetragem" Foreground="White"/>
                    <TextBox x:Name="txtMM"/>

                    <Label Content="Standard Pack" Foreground="White"/>
                    <TextBox x:Name="txtMOQ"/>

                    <Label Content="Estoque (m)" Foreground="White"/>
                    <TextBox x:Name="txtEstoque"/>

                    <Label Content="Metragem WIP" Foreground="White"/>
                    <TextBox x:Name="txtWIP"/>
                </StackPanel>

                <!-- Botões -->
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">
                    <Button Content="Salvar"
                            Click="Salvar_Click"
                            Style="{StaticResource PrimaryButton}"/>
                    <Button Content="Cancelar"
                            Click="Cancelar_Click"
                            Style="{StaticResource SecondaryButton}"
                            Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
